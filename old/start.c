#include <stdio.h>
#include <string.h>
 //system("CLS");
int main() 
{
     system("CLS");

  long num;
	char s[15];
	//scanf("%d", &num);
	gets(s);              	                  // Reading input from STDIN
	//printf("Input number is %d.\n", num*2);
	printf("%s",s[15]); 
   return 0;
}
